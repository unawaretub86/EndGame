const enumRole = {
  ADMIN: 'admin',
  STUDENT: 'Student',
  LEADER: 'leader'
};

const enumUserStatusByLeader = {
  PENDING: 'pending',
  AUTHORIZED: 'authorized'
};
export { enumRole, enumUserStatusByLeader };
